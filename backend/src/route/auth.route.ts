import { Router } from 'express';
// import { login, logout, signup, getCurrentUser, authenticate} from '../controller/auth.controller';

const router: Router = Router();

// router.route('/signup').post(bodyValidator(UserCreateDto), signup)
// router.route('/login').post(login);
// router.route('/logout').post(authenticate, logout);
// router.route('/currentUser').get(authenticate, getCurrentUser);

export default router;
